src/parameters.cpp: SKIP (already applied) 'void readParameters(ros::NodeHandle &nh)'
src/parameters.h: SKIP (already applied) 'void readParameters(ros::NodeHandle &n);'
src/preprocess.h: SKIP (already applied) '  bool given_offset_time;\n  ros::Publish'
src/preprocess.h: SKIP (already applied) '  void pub_func(PointCloudXYZI &pl, cons'
src/preprocess.cpp: SKIP (already applied) 'void Preprocess::pub_func(PointCloudXYZI'
src/laserMapping.cpp: SKIP (already applied) 'void publish_init_map(const ros::Publish'
src/laserMapping.cpp: SKIP (already applied) 'void publish_frame_world(const ros::Publ'
src/laserMapping.cpp: SKIP (already applied) 'void publish_frame_body(const ros::Publi'
src/laserMapping.cpp: SKIP (already applied) 'void publish_odometry(const ros::Publish'
src/laserMapping.cpp: SKIP (already applied) 'void publish_path(const ros::Publisher p'
src/laserMapping.cpp: SKIP (already applied) 'pubLaserCloudFullRes.publish(laserCloudm'
src/laserMapping.cpp: SKIP (already applied) 'pubLaserCloudFull_body.publish(laserClou'
src/laserMapping.cpp: SKIP (already applied) 'pubOdomAftMapped.publish(odomAftMapped);'
src/laserMapping.cpp: SKIP (already applied) 'pubPath.publish(path);'
src/laserMapping.cpp: SKIP (already applied) '    static tf::TransformBroadcaster br;\n'
src/laserMapping.cpp: SKIP (already applied) '    ros::init(argc, argv, "laserMapping"'
src/laserMapping.cpp: SKIP (already applied) '    ros::Subscriber sub_pcl = p_pre->lid'
src/laserMapping.cpp: SKIP (already applied) '    ros::Publisher pubLaserCloudFullRes '
src/laserMapping.cpp: SKIP (already applied) '    ros::Publisher pubLaserCloudMap = nh'
src/laserMapping.cpp: SKIP (already applied) '    signal(SIGINT, SigHandle);\n    ros::'
src/laserMapping.cpp: SKIP (already applied) '        status = ros::ok();\n        loop'
src/laserMapping.cpp: SKIP (already applied) '    fout_out.close();\n    fout_imu_pbp.c'
src/laserMapping.cpp: 3 x 'publish_odometry(pubOdomAftMapped);' -> 'publish_odometry(pubOdomAftMapped, tfBr)'
