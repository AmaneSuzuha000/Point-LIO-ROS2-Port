--- ~/pointlio_ros2_ws/src/livox_ros_driver2/CMakeLists.txt	2026-09-04 18:23:46.857175665 +0800
+++ livox2_ws/src/livox_ros_driver2/CMakeLists.txt	2026-09-05 12:48:42.140274801 +0800
@@ -237,7 +237,7 @@
   endif (APR_FOUND)
 
   # generate custom msg headers
-  set(LIVOX_INTERFACES livox_interfaces2)
+  set(LIVOX_INTERFACES livox_ros_driver2)
   rosidl_generate_interfaces(${LIVOX_INTERFACES}
     "msg/CustomPoint.msg"
     "msg/CustomMsg.msg"
@@ -257,7 +257,7 @@
   add_definitions(${PCL_DEFINITIONS})
 
   # livox ros2 driver target
-  ament_auto_add_library(${PROJECT_NAME} SHARED
+  ament_auto_add_library(livox_driver2_component SHARED
     src/livox_ros_driver2.cpp
     src/lddc.cpp
     src/driver_node.cpp
@@ -278,21 +278,21 @@
     src/call_back/livox_lidar_callback.cpp
   )
 
-  target_include_directories(${PROJECT_NAME} PRIVATE ${livox_sdk_INCLUDE_DIRS})
+  target_include_directories(livox_driver2_component PRIVATE ${livox_sdk_INCLUDE_DIRS})
 
   # get include directories of custom msg headers
   if(DISTRO_ROS STREQUAL "humble" OR DISTRO_ROS STREQUAL "jazzy")
     rosidl_get_typesupport_target(cpp_typesupport_target
     ${LIVOX_INTERFACES} "rosidl_typesupport_cpp")
-    target_link_libraries(${PROJECT_NAME} "${cpp_typesupport_target}")
+    target_link_libraries(livox_driver2_component "${cpp_typesupport_target}")
   else()
     set(LIVOX_INTERFACE_TARGET "${LIVOX_INTERFACES}__rosidl_typesupport_cpp")
-    add_dependencies(${PROJECT_NAME} ${LIVOX_INTERFACES})
+    add_dependencies(livox_driver2_component ${LIVOX_INTERFACES})
     get_target_property(LIVOX_INTERFACES_INCLUDE_DIRECTORIES ${LIVOX_INTERFACE_TARGET} INTERFACE_INCLUDE_DIRECTORIES)
   endif()
 
   # include file direcotry
-  target_include_directories(${PROJECT_NAME} PUBLIC
+  target_include_directories(livox_driver2_component PUBLIC
     ${PCL_INCLUDE_DIRS}
     ${APR_INCLUDE_DIRS}
     ${LIVOX_LIDAR_SDK_INCLUDE_DIR}
@@ -302,7 +302,7 @@
   )
 
   # link libraries
-  target_link_libraries(${PROJECT_NAME}
+  target_link_libraries(livox_driver2_component
     ${LIVOX_LIDAR_SDK_LIBRARY}
     ${LIVOX_INTERFACE_TARGET}   # for custom msgs
     ${PPT_LIBRARY}
@@ -311,9 +311,9 @@
     ${APR_LIBRARIES}
   )
 
-  rclcpp_components_register_node(${PROJECT_NAME}
+  rclcpp_components_register_node(livox_driver2_component
     PLUGIN "livox_ros::DriverNode"
-    EXECUTABLE ${PROJECT_NAME}_node
+    EXECUTABLE livox_driver2_component_node
   )
 
   if(BUILD_TESTING)
